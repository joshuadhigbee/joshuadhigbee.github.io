_site/


# === Secrets / credentials — never commit (managed: claude-secret-scan) ===
# Env files
.env
.env.*
!.env.example
!.env.sample
!.env.template
# Keys / certs (note: *.key also matches Apple Keynote files — use --no-verify if you must commit one)
*.pem
*.key
*.p12
*.pfx
*.keystore
*.jks
id_rsa*
id_ed25519*
# Common credential files (specific names, so content files like keeping-secrets.md are unaffected)
secrets.json
secrets.yml
secrets.yaml
secrets.toml
credentials.json
credentials.yml
credentials.yaml
client_secret*.json
service-account*.json
.netrc
.npmrc
.pypirc
.pgpass
# === end secrets block ===
